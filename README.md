# hubspot-tools

## hstools

```bash
% uv run hstools --help
Usage: hstools [OPTIONS] COMMAND [ARGS]...

Options:
  -s, --src_portal TEXT
  -d, --dst_portal TEXT
  --config TEXT
  --help                 Show this message and exit.

Commands:
  html
  show-config  hubspot config の表示
```
